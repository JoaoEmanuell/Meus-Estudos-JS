console.log("Vai começar")
for (var c=1; c<=10; c++){//o primeiro indice é a declaração de variavel no inicio, depois vem o fim, enquanto o fim não ser alcançado ele ira repertir o codigo, ja a ultima é o incremento, cada vez que o codigo for executado o incremento sera adicionado a ele, dessa forma modificando, quando a condição do fim for positiva o loop se encerra.
    console.log(c)
}
console.log('Fim')