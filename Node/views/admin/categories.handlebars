<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Categorias</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>
<body>
    {{>_navbar}}
    <div class="container">
        <div>
            {{>_msg}}
        </div>
        <h1 style="text-align: center; margin : 15px;">Lista de categorias</h1>
            {{#each categories}}
            <div class="card mt-4 mb-4">
                <div class="card-body">
                    <div class="card-title">
                        <h2>{{name}}</h2>
                    </div>
                    <p>{{date}}</p>
                    <p class="card-text">{{slug}}</p>
                    <p><a href="/admin/editcategories/{{_id}}" class="btn btn-success">Editar categoria</a></p>
                    <p><a href="/admin/removecategorie/{{_id}}" class="btn btn-danger">Deletar categoria</a></p>
                </div>
            </div>
            {{/each}}
        <a href="/admin/addcategories/"><button class="btn btn-success btn-lg">Nova categoria</button></a>
    </div>
</body>
</html>